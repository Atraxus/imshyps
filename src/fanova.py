# In this file, we implement the main functions of the fanova algorithm.

# import pandas as pd

def sample_hyperparameter_space(hyperparameter_space: list):
    # This expects a pd datafram with the following columns:
    #   - name: name of the hyperparameter
    #   - type: type of the hyperparameter (categorical, ordinal, numerical)
    #   - values: possible values of the hyperparameter
    #   - distribution: distribution of the hyperparameter (uniform, normal, lognormal)
    # And returns a high dimensional array with all the possible combinations of the hyperparameters.
    pass







def fanova():
    # samples = sample_hyperparameter_space(hyperparameter_space)
    # evaluate(samples)
    # functional_decomposition()
    # importance_analysis()
    pass 